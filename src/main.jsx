import React from 'react';
import ReactDOM from 'react-dom/client';
import SimonGame from './game.jsx';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Game />
  </React.StrictMode>
);
